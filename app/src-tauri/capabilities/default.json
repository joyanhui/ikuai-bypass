{
  "identifier": "default",
  "description": "Default capabilities for iKuai Bypass (desktop + mobile)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-emit",
    "core:event:allow-listen"
  ]
}
